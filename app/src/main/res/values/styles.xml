<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="XplocityColoredButton">
        <item name="android:textColor">@color/colored_button_text</item>
        <item name="android:background">@drawable/colored_button_background</item>
    </style>

    <style name="XplocityTextView" parent="TextAppearance.AppCompat.Display1">
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textColor">@color/gray900</item>
        <item name="android:layout_gravity">start</item>
    </style>

</resources>
